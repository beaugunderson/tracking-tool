{
  "github": {
    "release": true,
    "assets": [
      "dist/*.exe",
      "dist/*-mac.zip"
    ],
    "tokenRef": "RELEASE_IT_TOKEN"
  },
  "hooks": {
    "before:release": "rm -rf dist/ && yarn dist-mac && ./build-windows.sh"
  }
}
